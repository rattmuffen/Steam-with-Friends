{
    "name": "steam-with-friends",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "concurrently": "^4.0.1"
    },
    "scripts": {
        "start": "concurrently \"cd backend && npm install && node app.js\" \"cd client && npm install && npm start\""
    },
    "engines": {
        "node": "10.x"
    }
}
